# Analysis queries

`metrics.sql` calculates primary and secondary descriptive metrics for one experiment run.

`premature_retries.sql` calculates a strict premature-retry measure based on ground-truth supplier order creation time.

These are descriptive analysis queries only. Final paper analysis will add confidence intervals, effect sizes, regression models, and corrected pairwise tests after pilot data determines the observed distributions and power requirements.
